require "rails_helper"

RSpec.describe SlugController, type: :routing do
  context "valid slug" do
    pending "add some examples to (or delete) #{__FILE__}"
  end

  context "invalid slug" do
    pending "add some examples to (or delete) #{__FILE__}"
  end

  context "expired slug" do
    pending "add some examples to (or delete) #{__FILE__}"
  end
end
