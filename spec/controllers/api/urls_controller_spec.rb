require "rails_helper"

RSpec.describe Api::UrlsController, type: :request do
  context "when creating a new slug" do
    pending "add some examples to (or delete) #{__FILE__}"
  end

  context "when destroying a new slug" do
    pending "add some examples to (or delete) #{__FILE__}"
  end
end
